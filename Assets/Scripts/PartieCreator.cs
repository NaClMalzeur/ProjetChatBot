using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
    Classe permettant la création d'une partie
    Contient les méthodes pour renseigner différents paramètres par le joueur
    + choix du scénario
    Charger les options du scénario choisi 

 */
public class PartieCreator : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
}
